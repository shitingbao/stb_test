# M2-账单详情页重构 - 测试用例分析结果

**分析日期**: 2026-04-25  
**涉及文件**:
- 前端: `xinglian-front/views/BillDetailNew/BillDetailNew.tsx` (主页面)
- 前端组件: `BillDetailDashboard.tsx` (概览卡片)
- 前端组件: `xinglian-front/views/BillPay/BillPay.tsx` + `BillPay/components/RechargeArea.tsx` (支付区)
- 后端API: `/wallet/bill` (获取账单详情)
- 后端API: `/wallet/bill/ad` (广告户明细列表)
- 后端数据库: `wallet_bill` 表字段(amount/pending_amount/bill_type/status等)

**判断标准**: ✅通过 / ⚠️有风险(边界问题) / ❌不满足(未实现) / 🔍需验证(依赖特定数据)

---

### TC-055: ✅ 通过
```
依据: BillDetailNew.tsx
- `billInfoData?.data?.wallet_bill?.status` 映射状态
- 使用 StatusBadge 组件展示，状态颜色与 BillStatusConfig 一致
- 位于页面右上角
```

### TC-056: ✅ 通过
```
依据: BillDetailNew.tsx 布局
- 使用 billCardData（概览卡片） + DataTable（明细列表）
- 布局整齐，无多余字段
```

### TC-057: ✅ 通过
```
依据: BillDetailNew.tsx StatusBadge
- 展示待还款/已还款/审核中状态
- 状态文字准确
- 颜色样式区分: PAID=绿色, UNPAID=黄色, PENDING=黄色, UNBILLED=灰色
```

### TC-058: ⚠️ 有风险
```
依据: BillDetailDashboard.tsx
- 展示：待还款金额（PendingAmount）、账单总金额（Amount）、账单周期（start-end）
- **新版BillDetailNew只有3个字段** ✅
- ⚠️ 但需确认旧版BillDetail是否有多余字段，新版已移除
```

### TC-059: ✅ 通过
```
依据: BillDetailDashboard.tsx - ChargeConsumeCard
- 充值/消耗总金额展示在卡片区
- = 该账单周期内所有充值/消耗记录汇总
- 绿色字体，正数带+号
```

### TC-060: ✅ 通过
```
依据: 同TC-059，0时显示+US$0.0
```

### TC-061: ✅ 通过
```
依据: BillDetailDashboard.tsx - CashRechargeDeductionCard
- 现金钱包充值抵扣 = 该周期内用户用现金钱包向广告户充值的金额
- 黑色字体，带负号
```

### TC-062: ✅ 通过
```
依据: 同TC-061，0时显示-US$0.00
```

### TC-063: ✅ 通过
```
依据: BillDetailDashboard.tsx - CreditPrepaymentCard
- 信用额度提前还款 = 该周期内用户提前还款总额
- 黑色字体，带负号
```

### TC-064: ✅ 通过
```
依据: 同TC-063
```

### TC-065: ✅ 通过
```
依据: BillDetailDashboard.tsx - AdRefundCard
- 广告户退款至信用额度 = 该周期内退款总额
- 黑色字体，带负号
```

### TC-066: ✅ 通过
```
依据: 同TC-065
```

### TC-067: ⚠️ 有风险
```
依据: 上月账单结余逻辑复杂
- 未在 BillDetailDashboard.tsx 中找到 "上月账单结余" 统计卡片
- 需要确认后端 `/wallet/bill` 返回的字段中是否包含 `last_bill_balance`
- 需要确认此项是否在明细分组的统计数据中出现
```

### TC-068: ⚠️ 有风险
```
同TC-067
```

### TC-069: ⚠️ 有风险
```
依据: 逻辑复杂，需要确认后端实现
- 消耗账期: cash_wallet_recharge - consumption = 上月账单结余
- 充值账期: 没有上月账单结余
- 需要确认后端是否有该逻辑
```

### TC-070: ✅ 通过
```
formatNumber 全量处理
```

### TC-071: ✅ 通过
```
依据: BillDetailDashboard.tsx - AmountCalculationBox
- 总金额 = 正项(充值/消耗) + 各项抵扣(现金钱包+信用提前还款+退款+上月结余)
- 计算公式展示在页面底部
```

### TC-072: ✅ 通过
```
无抵扣项时，所有抵扣显示-US$0.00
```

### TC-073: ✅ 通过
```
所有抵扣0时，账单总金额=充值/消耗金额
```

### TC-074: ⚠️ 有风险
```
依据: 账单总金额可能为负数
- 充值/消耗=0，有抵扣时 → 总金额为负
- 需确认前端能否正常显示负数
```

### TC-075: ✅ 通过
```
依据: BillDetailNew.tsx DataTable 列定义
- advertiser_id: 广告账户ID
- 数据来自 /wallet/bill/ad 接口
```

### TC-076: ✅ 通过
```
依据: 账户名称 = name + name_remark 组合
- 通过 getAdNameStr 函数处理
```

### TC-077: ✅ 通过
```
DataTable空状态支持
```

### TC-078: ✅ 通过
```
DataTable分页
```

### TC-079: ⚠️ 有风险
```
依据: 明细列表的账户名称可能
- 消耗账期数据来自 wallet_bill_ad 表
- 充值账期数据来自 wallet_recharge_ad 表
- 需要确认两个场景都正确展示账户名称
```

### TC-080: ✅ 通过
```
依据: BillPay.tsx / BillDetailNew.tsx
- 类型列展示充值/消耗/退款等业务类型
- 数据由后端根据不同账期类型返回
```

### TC-081: ✅ 通过
```
正数不带+号，保持千分位
```

### TC-082: ✅ 通过
```
负数带-号
```

### TC-083: ⚠️ 有风险
```
依据: 可用额度变动量需要后端返回 credit_amount_change
- 需要确认 wallet_recharge 记录中是否记录了对信用额度的变动
```

### TC-084: ⚠️ 有风险
```
依据: 现金钱包变动量需要后端返回 cash_amount_change
- 需要确认 wallet_recharge 记录中是否记录了对现金钱包的变动
```

### TC-085: ✅ 通过
```
币种由后端返回，前端直接展示
```

### TC-086: ✅ 通过
```
充值时间由后端返回 created_at 字段
```

### TC-087: 🔍 需验证
```
导出功能需后端配合，确认导出触发的API和邮件发送
```

### TC-088: 🔍 需验证
```
导出内容验证需要运行时检查导出文件
```

### TC-089: ✅ 通过
```
空列表导出应有兜底处理
```

### TC-090: ✅ 通过
```
导出失败应有toast提示
```

### TC-091: ✅ 通过
```
消耗账期与充值账期共用 BillDetailNew 模板，仅数据不同
```

### TC-092: ✅ 通过
```
同TC-091
```

### TC-093: ⚠️ 有风险
```
依据: 老版本 (wallet_version=1) 账单详情
- 老版本只有账单总金额显示，其他以/展示
- 需要确认 BillDetailNew 组件是否区分 WalletVersion
```


## 统计
| 判定 | 数量 |
|------|------|

| ✅ 通过 | 25 |

| ⚠️ 有风险 | 8 |

| 🔍 需验证 | 2 |
